# Join Page Primary CTA Implementation Plan

1. Extend the Join Us page test to require an “立即加入” link pointing to `/join/apply` inside `#join-community`, not inside the cover.
2. Move the shared Button and semantic router link from the cover into the green stage.
3. Add a narrow Join-page CTA class: absolute lower-left placement on desktop, in-flow placement before the mascot on mobile, and an orange fill for contrast.
4. Run the focused test, production build, and desktop/mobile visual checks.
