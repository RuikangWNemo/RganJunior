# 首页 Hero 字标与橙色统一实施计划

1. 更新 `HeroCopy.tsx`，为中文标语增加两条明确的语义行。
2. 更新 `src/index.css`，将字标宽度调整为 80%、手机端上移 8px，并把 Hero 局部橙色统一到字标橙色变量。
3. 更新 `HomeHeroFlow.tsx` 的暖色渐变停靠点，并让吉祥物橙色外观与目标色相一致。
4. 补充首页测试，覆盖中文双行结构与英文结构不变。
5. 运行定向测试、Lint、生产构建，并在桌面和手机视口测量字标尺寸、位置、断行与溢出。
