# 加入我们：阿柑轮廓弧形提示实施计划

1. 将横排自动提示替换为包含桌面、手机两条路径的 SVG `textPath`。
2. 保留独立的屏幕阅读器 live status，避免 SVG 文字重复朗读。
3. 调整弧形文字、问询入口和展开面板的相对定位，使其贴近阿柑且不覆盖脸部。
4. 为英文长文案降低字号，保证弧线路径内可读且不被裁切。
5. 更新测试，验证弧形文字、常驻状态和身份面板行为。
6. 运行完整测试、Lint、生产构建及桌面／手机视觉检查。
