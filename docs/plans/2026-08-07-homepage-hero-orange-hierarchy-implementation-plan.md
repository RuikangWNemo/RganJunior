# Homepage Hero Orange Hierarchy Implementation Plan

1. Update the homepage hero color tokens and desktop/mobile composition offsets in `src/index.css`.
2. Increase the mascot breakpoint sizes by 20% and add a stable tooltip class in `src/components/home/HeroMascotStage.tsx`.
3. Style the primary CTA, top-nav Community entry, and mascot tooltip with one shared orange/forest treatment while preserving the secondary action treatment.
4. Extend focused tests to cover the mascot sizing hook and tooltip class where practical.
5. Run focused tests, ESLint, production build, diff checks, desktop/mobile computed-style verification, and service reachability checks.
