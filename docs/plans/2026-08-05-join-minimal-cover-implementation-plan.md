# 加入我们：极简封面实施计划

1. 从 `src/pages/JoinUs.tsx` 删除封面刊头、kicker、原说明、边注、folio 元信息，以及绿色区域索引标题。
2. 将封面内容替换为“成为阿柑少年 / 与我们同行”，保留橙色过渡曲线。
3. 在 `src/index.css` 收紧封面高度、简化为单列排版，并删除仅供已移除元素使用的样式。
4. 删除绿色索引后调整身份路径顶部间距，保留三身份、阿柑和档案交互。
5. 更新 `src/pages/JoinUs.test.tsx` 的封面断言与冗余文案缺席断言。
6. 运行定向测试、完整测试、Lint、生产构建、diff 检查与桌面／手机浏览器验证。
