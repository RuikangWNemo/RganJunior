# Navbar Menu Typography Implementation Plan

1. Update the shared desktop primary navigation link classes to include `font-bold` while retaining `text-sm`.
2. Remove the active-state `font-medium` class that would weaken the inherited bold weight.
3. Add a focused Navbar test covering active and inactive desktop menu labels.
4. Run the Navbar test suite and application typecheck.

