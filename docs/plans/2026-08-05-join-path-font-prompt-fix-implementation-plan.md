# 加入我们：字体、曲线与常驻提示实施计划

1. 将身份名称 CSS 字体切换为项目内置 `--font-handwriting-sans`。
2. 提高桌面与移动基础路径的不透明度和线宽，保留局部描亮层。
3. 将自动提示移到隐藏面板外，保持常驻显示。
4. 删除 `R’GAN’S MARGIN NOTE`，让展开面板只承载身份意图选择。
5. 调整定时器，使提示默认轮换、角色档案打开时暂停、减少动态时保持稳定。
6. 更新页面测试并完成自动化、构建与桌面／手机视觉验证。
