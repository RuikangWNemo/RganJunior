# 首页项目固定卡片实施计划

1. 扩展共享渐隐轮播，支持隐藏箭头、将自定义导航放到卡片下方，并在离开视口时暂停自动播放。
2. 将项目文字导航替换为四段可访问进度线，保留点击、触控、键盘与自动切换。
3. 调整项目板块背景、固定卡片结构、文案预留高度和移动端堆叠布局。
4. 将切换限制为 `opacity` 与 `transform`，补齐减少动态效果规则。
5. 更新首页测试，运行定向测试、应用类型检查、生产构建和桌面/移动浏览器验证。
