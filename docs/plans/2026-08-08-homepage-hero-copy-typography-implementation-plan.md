# 首页 Hero 文案排版实施计划

1. 在 `src/components/home/HeroCopy.tsx` 中为中英文品牌句增加语言状态类，并为英文加入语义明确的桌面端断行结构。
2. 在 `src/index.css` 中设置桌面端标题 `36px`、中文单行、英文两行，以及正文和按钮统一 `18px`；同时调整主按钮深绿色前景色。
3. 在首页测试中覆盖中英文标题结构和语言状态类。
4. 运行定向测试、应用类型检查与生产构建，并在桌面和手机视口验证中英文页面。
